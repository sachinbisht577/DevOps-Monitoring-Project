name: CI/CD Pipeline

on:
  push:
    branches: ["main"]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Login to DockerHub
        run: echo dckr_pat_1a2b3c4d5e6f7g8h9i0jklmnopqrstuv | docker login -u sachin --password-stdin

      - name: Build Image
        run: docker build -t sachinbisht577/monitoring-app:latest .

      - name: Push Image
        run: docker push sachinbisht577/monitoring-app:latest
